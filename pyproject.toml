[project]
name = "bedrock-api-feature-tests"
version = "0.1.0"
description = "Verify Anthropic API features on AWS Bedrock InvokeModel API"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.96.0",
    "boto3",
    "botocore>=1.43.26",
]
